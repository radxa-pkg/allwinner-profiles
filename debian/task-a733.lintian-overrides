# These wrappers replace diverted binaries whose manual pages are provided by
# the original packages.
task-a733: no-manual-page
# False positive: the maintainer scripts pass absolute paths to dpkg-divert;
# they do not execute those paths as commands.
task-a733: command-with-path-in-maintainer-script
