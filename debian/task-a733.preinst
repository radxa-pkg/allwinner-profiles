#!/bin/sh

set -e

case $1 in
install | upgrade)
	conflicts="/usr/bin/sddm-greeter-qt6"
	for i in $conflicts; do
		dpkg-divert --package task-a733 --divert "$i.bak" --rename "$i"
	done
	;;
esac

#DEBHELPER#

exit 0
