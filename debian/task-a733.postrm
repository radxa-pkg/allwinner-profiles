#!/bin/sh

set -e

#DEBHELPER#

case $1 in
remove)
	# workaround command-with-path-in-maintainer-script for /usr/bin
	conflicts="/usr/bin/sddm-greeter-qt6"
	for i in $conflicts; do
		dpkg-divert --package task-a733 --rename --remove "$i"
	done
	;;
esac

exit 0
